# Copy to `.env` beside lite.yml. Only BIGBOOK_ADMIN_EMAIL is required; the rest is defaulted or generated on first boot.
BIGBOOK_ADMIN_EMAIL=
BIGBOOK_ADMIN_PASSWORD=
BIGBOOK_BASE_URL=http://localhost:8080/
POSTGRES_PASSWORD=
KEYCLOAK_ADMIN_PASSWORD=
